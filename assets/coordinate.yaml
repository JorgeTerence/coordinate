# A custom header text
title: ""

# Details at https://open-props.style/#colors
color: red
alt: pink

# Allow nodes to upload files and zip archives to coordinate
upload: true

# Display QRCode on the terminal
qrcode: false

# Files and directories to filter (visual only!)
filters:
  - .gitignore
  - node_modules
  - package-lock.json
  - build
